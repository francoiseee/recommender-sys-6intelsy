# cnn_smoke.yaml — fast sanity config
model: cnn_ranker
seed: 42
batch_size: 128
epochs: 1
lr: 0.001
optimizer: adam
embed_dim: 64
num_filters: 32
filter_sizes: [2, 3, 4]
output_dim: 64
dropout: 0.3
early_stopping_patience: 1
max_len: 32
ablation_epochs: 1
